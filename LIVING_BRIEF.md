# MacroSnaps - Living Brief
Last updated: May 11, 2026 (Session 82: V2 confirmed as sole active codebase with V1 retirement planned for a separate session. Homepage radically simplified - 12 country card boxes removed entirely, snapshot table rows now serve as the navigation (whole row clickable to country page). Final homepage is just four boxes: header, headlines, snapshot, commodities. Snapshot table is now the centerpiece: default sort changed to GDP-rank descending via new GDP_RANK_ORDER constant (USA, CHN, DEU, JPN, IND, GBR, FRA, ITA, BRA, CAN, RUS, ZAF); flag chips added in country code cells (12 SVGs at 4:3 from lipis/flag-icons, inlined at build time, 22x16 rounded rect); typography bumped 12px to 13px with more row padding for thumb targets; cursor pointer + orange hover tint + country code goes orange-dark on hover + small right arrow fades in. New format_1dp() helper forces 1 decimal place on every snapshot cell ("2" to "2.0", "4.50" to "4.5"). Country page metric box title for Stock Market YTD now reads as "Stock Market" via DISPLAY_NAMES override (data.json key unchanged). Commodities filtered to Brent Crude / Natural Gas / Gold via COMMODITIES_DISPLAY constant with case-insensitive substring aliases (V1 pipeline still writes all 9). Chart x-axis label fix on country pages: monthLabelsFrom() in charts.js now emits "MMM 'YY" at every monthly position (was January-only with blanks elsewhere) so autoSkip has labels to pick from at every tick density; forecast-F detection tightened from includes to endsWith so "Feb '25" no longer triggers orange. Production cutover deployed at session close via python3 MACRO-MAY/build.py --target=production && git add v2/ && git commit && git push. Annual table view (pills for Growth, Inflation etc with years 2020-2026F as X-axis) explored - diagnostic confirmed GDP/Budget Deficit/Current Account have ready-made annual data in _frozen_historical, other 6 metrics would need year-end aggregation - deferred to a future session.)

Session 82 changes in detail:

(1) **V2 confirmed as sole active codebase.** All work going forward is on V2 only. V1 retirement planned but deferred to a separate session. Current state: MACRO-MAY/ is the V2 source folder (where build.py, styles.css, charts.js, flags/ live); v2/ is the deploy target (regenerated by build.py --target=production, served at macrosnaps.app/v2/). Both still sit inside the V1 repo at /Users/lisaswerling/RALPH/AI/macrosnaps/. Separate V2 repo with its own git remote, plus V1 deletion, scheduled as a standalone migration session.

(2) **Homepage radically simplified.** Removed the 12 country card boxes (3-bullet teasers, sourced from data.countries.<code>.stories.beginner) that had been on the homepage since Session 81. Briefly replaced with a 4x3 grid of rectangular flags (rounded corners, SVG library inlined) for one iteration before removing that too. Final homepage is 4 boxes: header (MacroSnaps + date), Today's headlines (3 numbered bullets from globalStories.beginner), Snapshot table (sortable, clickable rows), Commodities (3 items). No teasers, no duplication - the snapshot table is the navigation.

(3) **Snapshot table - now the centerpiece.** Multiple changes combined:
- **Default sort changed to GDP-rank descending.** New `GDP_RANK_ORDER` constant in build.py: `["USA","CHN","DEU","JPN","IND","GBR","FRA","ITA","BRA","CAN","RUS","ZAF"]` (2026 nominal GDP). Used only for the snapshot table's initial render. Column-click sorting in the browser still re-orders from there. Country pages still generated in `COUNTRY_ORDER` (geographic grouping).
- **All numeric cells forced to 1 decimal place.** New `format_1dp(val_str, force_sign=False)` helper. "2" becomes "2.0", "4.50" becomes "4.5", "+4.20%" becomes "+4.2", "-0.7" becomes "−0.7" (unicode minus). Signed columns prepend "+" for non-negatives so the column reads visually aligned.
- **Whole rows clickable to country page.** Click any row navigates. Click on `<th>` sorts that column. No conflict (different element types). Implemented via `data-href` attribute on each `<tr>` plus a small JS listener wired in the inline snapshot script block. Cursor pointer set explicitly on row and cells.
- **Stronger hover affordance.** Hover tint `rgba(249,115,22,.12)` (up from .05), country code goes `orange-dark` via `.code-text` class, small right arrow (`→`, color orange-dark, opacity 0 to 1) fades in next to the code on hover.
- **Flag chips next to country codes.** 22x16 rounded-rect flag SVG inlined in the country code cell. Reuses the `get_flag_svg()` helper. Adds visual identity per row at glanceable scale.
- **Typography bump.** Font size 12px to 13px, row padding 8px to 11px (top) plus 12px (bottom) for breathing room. Mobile padding bumped to 10-11px to hit ~44px tap target.
- **First column width** widened from 60px to 96px to accommodate flag + code + arrow.
- **Earlier experiments rejected.** Micro-bars (Tufte-style magnitude bars beneath each number) and direction triangles (▲/▼ next to signed values) were built and tested in one iteration, then removed. Visual noise without proportional reading benefit.

(4) **Country page metric box: "Stock Market YTD" displays as "Stock Market".** New `DISPLAY_NAMES = {"Stock Market YTD": "Stock Market"}` dict in build.py. Applied at the render layer in `render_metric_box()` for both the box `<h2>` header and the chart panel sub-title. The underlying data.json key `Stock Market YTD` is unchanged, preserving fetch_market_data.py, the homepage column, generate_digest.py, and RULE 15. The glossary block inside the same box still opens with "Stock Market YTD shows how much..." - not patched in this pass. Future polish.

(5) **Commodities filtered to 3.** New `COMMODITIES_DISPLAY` constant: `[["brent"], ["natural gas", "nat gas"], ["gold"]]`. Aliases are matched case-insensitively as substrings against `data.commodities.items[*].name`, so naming variants ("Natural Gas" vs "Nat Gas") work without a pipeline change. Display order on the page follows the order in this list. V1 daily ritual continues to write all 9 commodities (WTI Crude, Brent Crude, Natural Gas, Gold, Silver, Copper, Wheat, Corn, Soybeans) to data.json - V2 simply renders 3 of them.

(6) **Chart x-axis labels fixed.** All 7 line-based metric charts on country pages (Inflation, Unemployment, Policy Rate, Stock Market YTD, 10Y Bond Yield, Yield Curve, USD/DXY) were rendering with empty x-axes. Root cause in `charts.js`: `monthLabelsFrom()` emitted year labels only at January positions and empty strings everywhere else. Chart.js `autoSkip` then picked ~10 tick positions evenly spaced through the array; those positions almost never landed on the 5 January slots in a 60-point 5Y window, so what got rendered was the empty strings. Fix: emit `"MMM 'YY"` at every monthly position so autoSkip has labels at any tick density. 1Y view shows ~6 labels, 5Y view ~10, All view falls back to sparse. Bar charts (GDP, Budget Deficit, Current Account) were unaffected because they use the fully-populated ANNUAL_LABELS array. Also tightened the forecast-year orange-coloring detection from `label.includes('F')` to `label.endsWith('F')` so the new February labels ("Feb '25", etc) don't false-positive trigger the orange forecast color.

(7) **Flag asset library shipped.** Downloaded 12 SVGs at 4:3 from lipis/flag-icons (MIT licensed, github.com/lipis/flag-icons) into MACRO-MAY/flags/. ISO codes: us, ca, gb, jp, de, fr, it, cn, in, za, br, ru. Total payload ~13KB across 12 files (br.svg is the heaviest at 7KB due to Brazil's flag path complexity). New `get_flag_svg(country_code)` helper in build.py reads the SVG, strips root width/height attributes so CSS controls sizing in `.row-flag`, caches in `_flag_cache`. Currently used in snapshot table country code cells. The new `ISO_CODES` constant maps 3-letter codes to 2-letter ISO. Reusable for any future flag display.

(8) **MACRO-MAY/ vs v2/ folder confusion clarified.** MACRO-MAY/ is the V2 source code (build.py, styles.css, charts.js, flags/). v2/ is the deploy output (regenerated on every production build, served at macrosnaps.app/v2/, never hand-edited). The two folders are not redundant - they are source vs distribution. The future migration to a separate V2 repo will pick up MACRO-MAY/ + data.json + the daily ritual .py scripts as one unit; v2/ will be regenerated in the new repo.

(9) **Production deploy executed at session close.** All Session 82 changes pushed to macrosnaps.app/v2/ via the documented production workflow: `python3 MACRO-MAY/build.py --target=production && git add v2/ && git commit -m "..." && git push`. Continued use of `git add v2/` specifically (not `git add -A` or `git add .`) since the 28 stale HEADLINES/METRICS/harvest JSONs from Session 81 are still showing as deleted in git status pending a separate cleanup session.

(10) **Annual table view explored, deferred.** Concept: pills above the snapshot table swap views by metric (Snapshot, Growth, Inflation, Unemployment, Budget Deficit, Current Account, Policy Rate, Stock YTD, 10Y Bond, FX) with years 2020-2026F as the X-axis. Replicates V1's homepage table format. Diagnostic via `data['countries']['USA']['_frozen_historical']` confirmed: GDP Growth, Budget Deficit, Current Account already have clean annual arrays (27 values, 2000-2026F including forecast) ready to read directly. The other 6 metrics (Inflation, Unemployment, Policy Rate, 10Y Bond Yield, Stock Market YTD, FX) are stored as monthly arrays of 310-317 points and would need annual aggregation logic (year-end value for inflation/unemployment/rates/bonds, YoY % change for stock returns, year-end level for FX). Architecture sketched: 10 pills + a "Markets" divider, all tables generated at build time and inlined, table swap via CSS display toggle, sort and click-to-navigate preserved across views. Build deferred. When picked up, year-end vs year-average for monthly metrics is the only design decision left to make.

(11) **No daily ritual this session.** All changes are V2 cosmetic and structural (homepage simplification, snapshot table treatment, country page label override, commodities filter, chart label fix). Pipeline scripts untouched. data.json untouched. V1 site untouched.

(12) **No new standing rules.** Rule 15 still references the USA Stock Market YTD homepage column - that column still exists in V2 and the rule still applies. The "cp from Downloads" pattern using `"$(ls -t ~/Downloads/filename*.ext | head -1)"` with quoted subshell continues to be used throughout.

---

Session 81 changes in detail:

(1) **Strategic redesign approved and built end-to-end.** A conversation that began with "I want to completely overhaul the site" crystallised into a clear thesis: kill clicks, kill weather icons, kill mod/expert tiers, surface all numbers in one homepage table, make country cards on homepage just 3-bullet teasers, make country pages a single long scroll containing every metric. Architecture lock: two layers only (homepage and country page), no third click anywhere. The face card direction (Session 74) and MacroSushi rebrand were both set aside; the new direction borrows the headline format from MacroSushi but keeps the MacroSnaps name and the .app domain.

(2) **Aesthetic decisions.** Light mode chosen over dark. Page surface: warm-gray #f5f5f4. Card surface: white. Border: rgba(0,0,0,.06), border-radius 8px. Modular boxes throughout - every section sits in its own bounded white card on the warm-gray page surface. Primary accent: orange #F97316 (replaces cyan #00d4ff for chart lines, period pills, active states). Bullet-circle counters: Anthropic coral #CC785C (warmer than vivid orange, brand-anchor for the prose blocks). Darker orange for small interactive text and click arrows: #C2410C. Positive green #00b074 and negative red #d93a4f unchanged. Typography unchanged: Space Mono uppercase tracked for labels and tabular numbers, DM Sans for body. Metric names raised from small-label-size to the same large size as the value (28-30px) so the metric and its number read together as one heading.

(3) **Architecture and build system.** Multi-page static site: build.py generates 13 standalone HTML files into dist/. Each page inlines its CSS, JS, and relevant data - no runtime fetches, no external dependencies beyond Chart.js from cdnjs and Google Fonts. data.json is read from the parent macrosnaps repo (../data.json) so MACRO-MAY/ stays in sync automatically without duplication. Folder: /Users/lisaswerling/RALPH/AI/macrosnaps/MACRO-MAY/. Contains: build.py, styles.css, charts.js, serve.sh, README.md, dist/. Local server at [http://127.0.0.1:8765/](http://127.0.0.1:8765/) - offset from the headline review server's 8080, and 127.0.0.1 instead of localhost to dodge service-worker collisions.

(4) **Chart code ported verbatim from existing renderMetricChart.** The Chart.js config from macrosnaps-shell.html (lines around 55840-56016) reused as-is in MACRO-MAY/charts.js, adapted only by swapping dark color tokens for light: page bg from #091426 to white, grid lines from rgba(255,255,255,.06) to rgba(0,0,0,.05), tooltip bg from rgba(8,12,24,.97) to white. Cyan line replaced with orange #F97316. Period range pills (1Y/2Y/5Y/All, .mcr-btn class) match the existing pattern. Standard line metrics get a gradient-filled line. GDP, Budget Deficit, Current Account get a bar chart with positive (orange) / negative (red) coloring. Yield Curve keeps green-above-zero / red-below-zero segment coloring. Chart data sourced from data.json's _frozen_historical per country, same as the existing site.

(5) **Sortable snapshot table.** Countries as rows (12), metrics as columns (7): GDP, CPI, Unemp, Policy, 10Y, Stock YTD, FX YTD. All 8 columns (including the country code) clickable to sort. First click on a numeric column sorts descending (highest first); repeat-click flips to ascending. First click on country code sorts A-Z; repeat-click flips to Z-A. Active column header turns orange-dark with a small up/down arrow. Missing values always sink to the bottom regardless of direction. FX YTD column derived in build.py from existing data.json fields using compound math: fx_ytd = (1 + Stock_USD_YTD/100) / (1 + Stock_local_YTD/100) - 1, then x 100. USA stays blank since DXY is a basket index and USD-vs-USD is zero by definition.

(6) **Country cards on homepage.** Reduced to just 3 beginner-tier bullets per country - no metric rows, since the snapshot table already carries the numbers. Header row: country name only (3-letter pill removed in iteration). Whole card is a click-target linking to usa.html / can.html / etc. Coral circle counters on the bullets for visual continuity with the country page. [Note: Session 82 removed this block entirely.]

(7) **Country pages.** One per country (12 files). Header box: back link to homepage, date, country name + 3-letter pill. "Today" box: 3 country-level beginner stories. Then 10 metric boxes: GDP Growth, Inflation (CPI), Unemployment, Budget Deficit, Current Account, Policy Rate, Stock Market YTD, 10Y Bond Yield, Yield Curve, FX. Each metric box: metric name + value (both 28-30px), 3 beginner story bullets with coral circles, chart panel (history-since-2000 line with 1Y/2Y/5Y/All toggles, or annual bar for structural metrics), glossary block (What is X? + body + source attribution).

(8) **What's killed in v2.** Mod/expert tiers no longer rendered (data.json still carries them for backwards compatibility; backend story generation in update_metric_stories.py still produces all three tiers - to be ripped out in a future session for Haiku token savings). All weather icons gone. Third-click dashboard layer dissolved. Original homepage's stories/movers/connections format replaced by a simpler 3-bullet headlines block at the top, sourced from data.globalStories.beginner (first bullet of each of the 3 items).

(9) **Known v1 stubs.** No favicon, no OG meta tags, no analytics. Killing mod/expert from the backend pending; would compound Haiku savings on top of Session 76 trigger system.

(10) **Service worker gotcha discovered.** Repeated "cards.json failed to load" errors on localhost:8765 traced to a service worker registered by Ralph's fundaxhosa app for the localhost scope. The SW intercepts every localhost:* request regardless of port. Workaround: serve at 127.0.0.1:8765 instead (different origin, SW does not apply). Permanent fix: DevTools to Application to Service Workers to Unregister anything for localhost. Worth knowing for any future localhost-served project.

(11) **No daily ritual; no commits to master from MACRO-MAY work itself.** Strategy and prototype build session. All work is in MACRO-MAY/ subfolder; main macrosnaps build is untouched. The v2 production output (see below) is the first thing pushed.

(12) **Production cutover decision: parallel deploy at macrosnaps.app/v2/.** Live site stays at root (v1 unchanged). v2 lives at /v2/ for several weeks of tightening and public testing. Once stable, v1 retires: delete the old build.py and macrosnaps-shell.html, rename MACRO-MAY/build.py to root build.py, point its output back at root instead of /v2/, strip moderate and expert from the story generator. Pipeline scripts unchanged throughout the transition - v2 is purely a new shell.

(13) **build.py --target flag shipped.** MACRO-MAY/build.py now accepts --target=local (default, writes to MACRO-MAY/dist/ for the local dev server) or --target=production (writes to ../v2/ for the parallel deploy). Same single source of truth, two output destinations. Local serve workflow unchanged. Production deploy is a one-line command followed by a clean git add v2/ + commit + push.

(14) **FX regime block wired from existing data.json.** Discovered fxRegime field is already present in data.json for all 12 countries (label + beginner + moderate + expert text). No migration script needed. MACRO-MAY/build.py now reads country.fxRegime.beginner and renders it as an orange-tinted callout inside the FX metric box on each country page (label format: "[Country]'s FX regime: [label]"). Adds ~1 KB per country page.

(15) **Pre-existing repo state cleanup deferred.** 28 stale HEADLINES_*, METRICS_*, and harvest_* JSON files (covering 2026-04-27 through 2026-05-07) show as deleted in git status. These are operational artefacts from the daily ritual that should never have been tracked. Cleanup approach (deferred to a separate session): add the three filename patterns to .gitignore, then git rm --cached them. This v2 deploy commit will be kept clean by using "git add v2/" specifically (not git add -A).

---

[Earlier session entries 80 down to 45 preserved in full in the prior version of this brief; collapsed here for length. See the full session history at the bottom of this document.]

---

## Standing rules

### ABSOLUTE NON-NEGOTIABLE OUTPUT RULES

These apply to EVERY SINGLE RESPONSE. No exceptions. Ever. Listed verbatim below; Ralph adds to this list session by session.

**RULE 1 - File downloads always to ~/Downloads/. Always provide the bash copy command using the latest-file pattern.**
After presenting any file for download, always provide the bash command to copy it into the correct repo path. The copy command must use:
```bash
cp "$(ls -t ~/Downloads/filename*.ext | head -1)" /Users/lisaswerling/RALPH/AI/macrosnaps/filename.ext
```
The `$(ls -t ... | head -1)` subshell grabs the most recently downloaded version. Quotes around the subshell are mandatory to handle macOS filenames containing spaces. Then provide the run command. In that order. Always.

**RULE 2 - URLs always as clickable markdown links.**
Every URL in every response must be a clickable markdown link. Never paste a bare URL. Example: [http://localhost:8080](http://localhost:8080) - never `http://localhost:8080` as plain text.

**RULE 3 - Always show a plan before building. Never start without it.**
Before writing any code, generating any file, or making any change to the repo, show the plan first. The plan should be short and concrete: what will change, what files are touched, what the user will see at the end.

**RULE 4 - Always ask for confirmation after showing the plan. Wait for the go-ahead.**
After presenting the plan, stop. Do not proceed until Ralph has confirmed. No "I'll go ahead and start" momentum.

**RULE 5 - All text must never look AI-written.**
No em-dashes (use regular hyphens). No apostrophes inside JSON summary fields. No "delve", "tapestry", "boasts", "robust", "leverage" (verb), "in conclusion", "it's important to note", or other LLM tells. Plain, human, readable English. Specific over vague. Numbers over hand-waves.

**RULE 6 - Add to brief when asked. Never present the brief as a download unless asked.**
When Ralph says "add this to the brief" or "remember this", add it silently. Do not produce the revised brief unless he explicitly asks for it.

**RULE 7 - The brief is always delivered as a .md file for download.**
When Ralph asks for the updated brief, produce a `.md` file (not Word, not PDF, not inline text). Save to `~/Downloads/` so it follows Rule 1.

**RULE 8 - When asked for bash code, respond with just the code block. No preamble.**
If Ralph asks for a command, give the command. No "Here's the command:". No "This will do X:". Just the fenced code block. After the block, the cp + run command pair if relevant (Rule 1).

**RULE 9 - Do not push to git during development.**
Push only when session work is complete and Ralph has confirmed the push. Build locally, preview locally, ship only when explicitly told to.

**RULE 10 - When you need to see a file from the local repo, give the bash code to copy it to Downloads, then wait for Ralph to upload it.**
Claude cannot read Ralph's local filesystem. The flow is: Claude asks for the file, gives a `cp /Users/lisaswerling/RALPH/AI/macrosnaps/path/to/file ~/Downloads/` command, Ralph runs it and uploads the file from Downloads to the chat.

**RULE 11 - When asked to add a rule, add it to the brief silently. Do not present the revised brief unless asked.**
Same as Rule 6 but for rules specifically. The acknowledgement is the addition. No restating the rule back, no "I've added that".

**RULE 12 - Never ask two questions at the same time.**
One question per turn, maximum. If clarification needs more than one question, ask the most important one first and follow up after the answer.

**RULE 13 - Ralph never manually edits files. Claude does it.**
If a file needs a patch, Claude writes a patch script, delivers it to Downloads, gives the cp + run commands. If Claude needs to see the current state of the file first, use Rule 10. Ralph does not open files in an editor.

**RULE 14 - There is no Lisa.**
"Lisa" is the name of Ralph's MacBook (which is why home is `/Users/lisaswerling/`). Ralph is the operator, designer, owner, and only human in the loop. All language in briefs and responses refers to Ralph. The directory path stays unchanged - it's just a hostname.

**RULE 15 - USA Stock Market YTD (USD) must equal local YTD verbatim.**
On the homepage Stock Market YTD ranking, the USA row's USD column (col2) must display identically to the local column (col1). USA's local currency is USD, so any FX adjustment is conceptually zero. In fetch_market_data.py, the USA branch parses the local YTD string back to a float and stores that as the USD value. Never apply DXY or any other FX-adjusted calculation to USA. Other countries continue to use the standard compound formula.

### Other standing notes

**Local preview before push.** Since the site is live, all changes must be tested locally before pushing. Always provide a local preview step before any git push command. Never combine build and push.

**Plan-confirm-build-test-ship loop.** Standard cycle: Claude shows plan → Ralph confirms → Claude builds (delivers files via Downloads + cp commands) → Ralph runs locally → both review → Ralph confirms → Claude pushes (or gives push command).

**V2 is the only active codebase.** All future work happens in MACRO-MAY/ (source) and v2/ (deploy). V1 is being retired. Migration to a separate V2 repo with its own git remote is pending and will happen in a standalone session.

---

## Daily Bash Ritual

**Friday only - run this first, before anything else:**
```bash
cd /Users/lisaswerling/RALPH/AI/macrosnaps && python3 forecast_server.py
```
Open `forecast_cms.html` directly from the filesystem (the Flask server only serves API endpoints, not the static page):
```bash
open /Users/lisaswerling/RALPH/AI/macrosnaps/forecast_cms.html
```
Review and update forecasts, then close the server before proceeding.

Run the consolidated pre-review script (Session 79: replaces the six individual python3 commands that used to be run step-by-step):

```bash
cd /Users/lisaswerling/RALPH/AI/macrosnaps && bash morning_bash.sh
```

`morning_bash.sh` runs the following six steps in sequence with `set -e` (halts on first error), step banners, and tee'd logging to `logs/morning_bash_YYYY-MM-DD.log`:

```
python3 fetch_market_data.py
python3 sync_market_historical.py --apply
python3 sync_commodity_data.py --apply
python3 update_commodity_stories.py
python3 update_headlines.py
python3 update_metric_stories.py
```

On Fridays, the script prompts to confirm `forecast_server.py` has been run first; "n" hard-exits and reminds you to run the forecast server before re-running morning_bash.sh.

Manual gate (headlines only - metric stories gate retired Session 78): open `headline_review.html` (via [http://localhost:8080](http://localhost:8080)), load `HEADLINES_draft_YYYY-MM-DD.json`, review and edit, export `HEADLINES_approved_YYYY-MM-DD.json`. The browser save dialog (File System Access API, Session 71) writes directly into the repo - no `mv` step needed.

Metric stories now apply directly from the draft file - no review UI, no rename step.

```bash
python3 update_headlines.py --apply HEADLINES_approved_YYYY-MM-DD.json
python3 update_metric_stories.py --apply METRICS_draft_YYYY-MM-DD.json
python3 build.py
cd /Users/lisaswerling/RALPH/AI/macrosnaps && python3 audit_ritual.py
```

Note: to open the headline review UI locally without CORS errors, run `python3 -m http.server 8080` in the macrosnaps directory first, then use [http://localhost:8080](http://localhost:8080).

### Intraday Bash Ritual (ad hoc, news-driven)

Run when markets are moving fast and the global story needs refreshing mid-day. Can be run 2-4x per day. No review gates. Takes 2-3 minutes.

```bash
cd /Users/lisaswerling/RALPH/AI/macrosnaps
python3 fetch_market_data.py --apply
python3 sync_commodity_data.py --apply
python3 update_global_stories.py
python3 build.py --apply
```

---

## Cards system - aborted (Session 75)

The hand-drawn face / cards / animation redesign locked in Session 74 was built to Phase 1 in Session 75 and then aborted. The static-card concept rendered cleanly but the animation gap could not be closed without a much denser hand-drawn library, and the cards themselves added complexity without a clear pull from readers.

Concept is dead. Do not revive without a fresh strategic conversation. The existing dashboard architecture (weather icons, three-act stories, complexity tiers, daily ritual) remains the canonical product. See Session 75 detail above for what was built and removed.

---

## MacroSnaps product overview

MacroSnaps (macrosnaps.app) is a free daily macro and markets dashboard covering 12 major economies with data explained at beginner / moderate / expert levels. Built by Ralph Lazar, former macro strategist at Goldman Sachs and fixed-income prop trader at CSFB. Educator landing page: macrosnaps.app/educators.html.

As of Session 81 the V2 dashboard ships at macrosnaps.app/v2/ as a parallel deploy. V1 stays at root for now; V2 takes over after several weeks of tightening. As of Session 82, all active development work is on V2 only and V1 is being retired. The face / cards redesign attempted in Sessions 74-75 was aborted; existing pipeline preserved unchanged.

---

## Architecture notes (latest state)

Key invariants:
- sync_sheet.py --apply writes only annual forecast fields
- sync_monthly_actuals.py writes only the monthly_actuals field
- These two scripts must never touch each other's fields
- build.py fails with a clear error if value_at_generation differs from current value for any metric story

### Header date (as of Session 68)
- The top-left "Updated" date in `macrosnaps-shell.html` is set by a JS IIFE using `new Date()` in UTC
- It always reflects today's GMT date when the page loads - it does not read from `data._meta.generated`
- The site never looks stale between builds

### _frozen_historical alignment (as of Session 66)
- Every monthly `_frozen_historical` series carries a `"startDate": "YYYY-MM"` field
- The JS left-aligns from `startDate`: data is placed at the correct label position regardless of array length or build date
- Live series (Yield Curve, Bond Yield, Stock Market, FX) are kept at 316 pts by `sync_market_historical.py` and are always current
- Inflation (CPI) is rebuilt from the MACRO-MONTHLY sheet via `rebuild_cpi_historical.py` - re-run this whenever the sheet is materially updated
- Live series remain current; frozen series show a null gap after their freeze date
- Any future series added to `_frozen_historical` must include a `startDate`

**Known data quality gaps - require FRED backfill:**
- **IND 10Y Bond Yield** - only 2 data points; placeholder array; needs FRED backfill
- **IND Yield Curve** - only 2 data points; placeholder array; needs FRED backfill

### Story formula (three-act global arc)
- Card 1 - The Trigger: one economic event or data print driving global attention
- Card 2 - Biggest Movers: which markets, currencies, or economies are reacting and how
- Card 3 - The Connection: what ties cards 1 and 2 together, the "so what" for the global picture

All three levels (beginner, moderate, expert) tell the same arc at different depths.

### Editorial principle: forecasts vs stories
Forecast values (source: Ralph's Google Sheet) are annual consensus views for 2026. They drive the metric value and weather icon. Stories should be written off recent data and trends, not off the forecast values.

### JSON file naming (as of Session 59)
- `harvest_YYYY-MM-DD.json` - raw web search data; internal only; consumed by `update_metric_stories.py`; never loaded in a UI
- `HEADLINES_draft_YYYY-MM-DD.json` - country card + global stories draft; loaded in `headline_review.html`
- `HEADLINES_approved_YYYY-MM-DD.json` - approved headlines; applied via `update_headlines.py --apply`
- `METRICS_draft_YYYY-MM-DD.json` - per-metric bullet stories draft; loaded in `metric_story_review.html`
- `METRICS_approved_YYYY-MM-DD.json` - approved metric stories; applied via `update_metric_stories.py --apply`

### MACRO-MONTHLY sheet (as of Session 60)
- Sheet ID: `1-s4hppAkoTZbjGGEkHSUDK2H7E00RHhVuHrYKWLuHpI` (env var: `MACRO_MONTHLY_SHEET_ID`)
- Three tabs: Inflation, Unemployment, Policy_Rate
- Unemployment sources by country:
  - USA: FRED `UNRATE` (BLS, monthly)
  - CAN/JPN/DEU/FRA/ITA/RUS: IMF LS dataset (monthly)
  - GBR: FRED `LRHUTTTTGBM156S` (ONS via FRED, monthly)
  - BRA: IBGE SIDRA table 6381 (PNAD Contínua, monthly) - wired into `update_monthly_actuals.py`
  - ZAF: FRED `LRUNTTTTZAQ156S` (OECD quarterly, interpolated) - backfilled via `backfill_unemployment.py`
  - IND: World Bank `SL.UEM.TOTL.ZS` (annual, interpolated) - backfilled via `backfill_unemployment.py`
  - CHN: no free programmatic source - permanent blank
  - RUS 2000-2009: no free programmatic source - permanent blank

---

## MacroSnaps v2 (MACRO-MAY) - parallel deploy at macrosnaps.app/v2/ as of Session 81 (homepage simplified in Session 82)

A light-mode redesign of MacroSnaps. v1 stays live at the root; v2 lives at `/v2/` for several weeks of tightening, after which v1 retires. Pipeline scripts (the daily ritual) unchanged throughout - v2 is purely a new shell on top of the same `data.json`.

Lives at `/Users/lisaswerling/RALPH/AI/macrosnaps/MACRO-MAY/` as a subfolder of the main repo. Subfolder structure: `build.py`, `styles.css`, `charts.js`, `serve.sh`, `README.md`, `dist/` (local dev output), `flags/` (12 SVG flag assets at 4:3 from lipis/flag-icons).

### Architecture

- Multi-page static site: one `index.html` (homepage) plus 12 country pages (`usa.html`, `can.html`, `gbr.html`, `jpn.html`, `deu.html`, `fra.html`, `ita.html`, `chn.html`, `ind.html`, `zaf.html`, `bra.html`, `rus.html`).
- Generated by `MACRO-MAY/build.py` which reads `../data.json` (the parent macrosnaps repo's canonical data) plus `styles.css`, `charts.js`, and the SVG files in `flags/`, all from the same folder.
- Each generated page inlines all CSS, JS, and relevant data (including flag SVGs in column 1 of the snapshot table). Standalone files, no fetch at runtime. Only external dependencies are Chart.js from cdnjs and Google Fonts.
- `build.py` accepts a `--target` flag with two values:
  - `--target=local` (default): writes to `MACRO-MAY/dist/` for the local dev server.
  - `--target=production`: writes to `../v2/` (i.e. `macrosnaps/v2/`) for the parallel deploy.

### Visual identity

- Light mode (white card surfaces on warm-gray `#f5f5f4` page surface).
- Modular box system: every section in its own bordered white card with `border-radius: 8px` and `border: 1px solid rgba(0,0,0,.06)`.
- Primary accent: orange `#F97316`. Darker variant for small interactive elements and click arrows: `#C2410C`. Bullet-circle counters: Anthropic coral `#CC785C`.
- Positive green `#00b074`, negative red `#d93a4f` (unchanged from existing site).
- Typography: Space Mono uppercase tracked for labels and tabular numbers; DM Sans for body. Two weights only (400 regular, 500 medium).
- Metric headings (name + value) at 28-30px in DM Sans 500 sentence case; section labels at 10-12px Space Mono uppercase.
- Snapshot table at 13px Space Mono (was 12px in Session 81, bumped in Session 82).

### Homepage structure (top to bottom) - Session 82

Four boxes total:

1. **Header box.** "MacroSnaps" + date stamp.
2. **"Today's headlines" box.** 3 numbered bullets (coral circles), sourced from `data.globalStories.beginner` (first bullet of each of the 3 items).
3. **"Snapshot" box.** Sortable table, 12 country rows by 7 metric columns (GDP, CPI, Unemp, Policy, 10Y, Stock, FX YTD). Each row carries a 22x16 rounded-rect flag chip next to the country code. Default sort: GDP-rank descending (USA, CHN, DEU, JPN, IND, GBR, FRA, ITA, BRA, CAN, RUS, ZAF). All 8 columns clickable to sort. Descending-first for numeric, A-Z for country code. Missing values sink to bottom. All numeric cells formatted to 1 decimal place via `format_1dp()`. Whole rows clickable to that country's page (cursor pointer, orange hover tint, country code goes orange-dark, small right arrow fades in next to the code on hover). Column header sort and row click do not conflict (different element types).
4. **Commodities box.** 3 items: Brent Crude, Natural Gas, Gold. Filtered from data.json's 9-item commodities list via the `COMMODITIES_DISPLAY` alias matcher.

The 12 country card boxes from Session 81 (3-bullet teasers) and the brief Session 82 flag grid (12 flag tiles in a 4x3 layout) are both removed. The snapshot table itself is the navigation.

### Country page structure (top to bottom, long vertical scroll)

1. Header box: back link to homepage, date, country name + 3-letter pill.
2. "Today" box: 3 country-level beginner stories.
3. 10 metric boxes (GDP Growth, Inflation (CPI), Unemployment, Budget Deficit, Current Account, Policy Rate, Stock Market, 10Y Bond Yield, Yield Curve, FX). Each box: metric name + value (28-30px), 3 beginner story bullets, chart panel (line with 1Y/2Y/5Y/All toggles for time series, bar for annual structurals), glossary block, source attribution. The Stock Market box title (and its chart sub-title) reads as "Stock Market" rather than "Stock Market YTD" via the `DISPLAY_NAMES` override; the underlying data.json key stays "Stock Market YTD".
4. The FX metric box additionally surfaces an orange-tinted callout from `country.fxRegime.beginner` titled `[Country]'s FX regime: [label]`.

### Snapshot table column derivation

- **FX YTD vs USD** (snapshot table column): computed in `build.py` from existing fields. Formula: `(1 + stock_usd_ytd/100) / (1 + stock_local_ytd/100) - 1`, then x 100. USA shows blank because DXY is a basket index, not a single FX pair.
- **Snapshot value formatting** (Session 82): all numeric cells routed through `format_1dp()`. Signed columns (GDP, Stock, FX) prepend `+` for non-negatives. Negatives always render with unicode minus.

### Flag assets (added Session 82)

- 12 SVG files at 4:3 in `MACRO-MAY/flags/`. ISO codes (lowercase 2-letter): us, ca, gb, jp, de, fr, it, cn, in, za, br, ru.
- Source: [lipis/flag-icons](https://github.com/lipis/flag-icons) (MIT licensed). Pulled via `curl` from `https://cdn.jsdelivr.net/gh/lipis/flag-icons@main/flags/4x3/{iso}.svg`.
- Total payload ~13KB across 12 files.
- Inlined into the snapshot table country code cells at build time via `get_flag_svg(country_code)` helper. Helper strips the root `width=` and `height=` attributes from each SVG so CSS controls sizing via `.row-flag { width: 22px; height: 16px; }`. Cached in `_flag_cache` dict.
- `ISO_CODES` constant maps 3-letter country codes to 2-letter ISO. Reusable for any future flag display.

### Constants summary (current state)

- `COUNTRY_ORDER`: geographic grouping. Used for country page generation and any per-country iteration where rank doesn't matter.
- `GDP_RANK_ORDER`: 2026 nominal GDP descending. Used for the snapshot table default sort.
- `ISO_CODES`: 3-letter to 2-letter ISO map for flag filenames.
- `COMMODITIES_DISPLAY`: list of alias lists for the V2 commodity filter.
- `DISPLAY_NAMES`: render-layer label overrides (currently just "Stock Market YTD" to "Stock Market").
- `SNAPSHOT_COLS`: column tuples (label, group, metric_key, fmt) for the snapshot table.
- `MACRO_METRICS`, `MARKET_BASE`, `FX_KEYS`: per-country metric ordering for country pages.

### Local dev workflow

```bash
cd /Users/lisaswerling/RALPH/AI/macrosnaps/MACRO-MAY && bash serve.sh
```

Serves `dist/` on port 8765. Use [http://127.0.0.1:8765/](http://127.0.0.1:8765/) (not `localhost`) to dodge any service worker collisions on the localhost scope.

### Production deploy workflow

```bash
cd /Users/lisaswerling/RALPH/AI/macrosnaps
python3 MACRO-MAY/build.py --target=production
git add v2/
git commit -m "v2 update"
git push
```

GitHub Pages publishes 1-3 minutes later at [https://macrosnaps.app/v2/](https://macrosnaps.app/v2/). Run this manually whenever you want v2 changes deployed. Daily ritual is **not** yet wired to rebuild v2 - that integration is deferred until v2 stabilises.

**Always use `git add v2/` specifically**, never `git add -A` or `git add .`, until the pre-existing repo cleanup is done (Session 81 noted 28 stale HEADLINES/METRICS/harvest JSON files marked as deleted; cleanup deferred to a separate session). Staging selectively keeps the v2 commit clean.

### What's killed vs the existing site

- Moderate and Expert story tiers (UI only; backend still generates all three tiers in the daily ritual).
- All weather icons.
- Third-click dashboard layer (the existing detailed country view).
- Original homepage stories / movers / connections format.
- 12 country card boxes (Session 81) and 12-tile flag grid (Session 82 iteration) - both removed; snapshot rows are the navigation.
- 6 of 9 commodities (Session 82): only Brent Crude, Natural Gas, Gold render.

### Known v1 stubs and parked work

- No favicon, no OG meta tags, no analytics on v2.
- Mod/expert backend generation still runs in daily ritual. Killing it would compound Haiku token savings on top of the Session 76 trigger system. Pending until v2 takes over fully.
- Daily ritual not yet wired to rebuild v2. Manual rebuild-and-push for now (production deploy workflow above).
- V1 site still live at root. Migration to a separate V2 repo with its own git remote pending.
- Glossary block inside the "Stock Market" metric box on country pages still says "Stock Market YTD shows how much..." - the title was renamed in Session 82 but the glossary body wasn't.
- Annual table view (pills for Growth, Inflation etc with years 2020-2026F as X-axis) scoped in Session 82 - architecture sketched and aggregation rules drafted - deferred. When picked up, year-end vs year-average for monthly metrics is the only design decision left.

---

## Substack strategy

**Goal for first 3 months:** audience building only. No paywall, no monetisation pressure.

**Cadence: daily posts + weekly digest**

- Daily post: short, low-friction, almost verbatim from the pipeline's three-act global story output. Proves the pipeline is alive, creates a scrollable archive, feeds Substack's algorithm.
- Weekly digest: longer and more considered; what mattered this week, which countries moved, what surprised, what to watch next week.

Keep daily posts genuinely short.

**Funnel: glossary links + one CTA per post.** Every glossary term hyperlinked to live glossary on macrosnaps.app (first occurrence per post only). One explicit CTA at end of every post.

**Voice:** Substack readers follow people, not products. A 2-3 sentence human intro each day (written by Ralph) is important.

**Promotion (first 3 months), in priority order:**
1. Personal outreach - first 50-100 subscribers from known network
2. LinkedIn - weekly post with chart or insight + link
3. Cross-recommendations on Substack
4. Twitter/X - slow burn, worth maintaining for macro community
5. Ask early subscribers to forward to one person

What not to bother with in first 3 months: paid promotion, SEO, press outreach, ProductHunt-style launches.

Do not position against Bloomberg or data terminals. The competition is a good morning read, not a data subscription.

---

## Full session history

Session 82: V2 confirmed as sole active codebase (V1 retirement deferred to a standalone migration session). Homepage radically simplified - 12 country card boxes removed entirely, brief Session 82 flag-grid iteration also removed, snapshot table rows are now the navigation. Four homepage boxes total: header, headlines, snapshot, commodities. Snapshot table treatment: GDP-rank descending default sort via new GDP_RANK_ORDER constant; 22x16 rounded-rect flag chips next to country codes (12 SVGs at 4:3 from lipis/flag-icons inlined at build time, helper get_flag_svg() strips root width/height for CSS sizing); typography bumped 12px to 13px with more padding; cursor pointer, orange hover tint, country code goes orange-dark, right arrow fades in on hover; whole row clickable to country page via data-href attribute and a small JS listener (column header click sorts, row click navigates, no conflict due to different element types); all numeric cells routed through new format_1dp() helper for 1-decimal formatting. Country page metric box title "Stock Market YTD" renamed to "Stock Market" via new DISPLAY_NAMES override (data.json key unchanged); glossary body inside the same box still says "Stock Market YTD shows how much..." (deferred). Commodities filtered to Brent Crude / Natural Gas / Gold via new COMMODITIES_DISPLAY constant with case-insensitive substring aliases supporting "Natural Gas" or "Nat Gas" naming variants (V1 pipeline still writes all 9). Chart x-axis label fix: monthLabelsFrom() in charts.js was emitting year labels only at January positions (empty strings elsewhere), Chart.js autoSkip then picked positions that didn't land on those slots so all 7 line charts on country pages had empty x-axes; fix emits "MMM 'YY" at every monthly position; forecast-F detection tightened from includes to endsWith so "Feb '25" no longer false-positive triggers orange. Earlier in session: micro-bars and direction triangles built and tested in the snapshot table, then removed (visual noise without proportional benefit). Annual table view (pills for Growth/Inflation/etc with 2020-2026F years on X-axis) scoped: GDP/Budget Deficit/Current Account have clean annual data ready in _frozen_historical, other 6 metrics would need year-end aggregation; architecture sketched (10 pills, all tables inlined at build, table swap via display toggle), build deferred. Production deploy executed at session close.
Session 81: MacroSnaps v2 prototype designed, built, and parallel-deployed at macrosnaps.app/v2/. Multi-page static site (homepage + 12 country pages) generated by MACRO-MAY/build.py from ../data.json. Light mode, modular boxes, orange (#F97316) and Anthropic coral (#CC785C) palette; Space Mono labels and DM Sans body preserved from existing site. Chart code ported verbatim from existing renderMetricChart (cyan to orange, dark to light bg). Homepage: 3-bullet headlines block + sortable snapshot table (12 rows x 7 cols, all 8 columns clickable, missing values sink to bottom, FX YTD column derived as (1+usd)/(1+local)-1 from existing Stock YTD fields) + 12 country card boxes (3 beginner bullets each, click-through to country page) + commodities box. Country page: header + Today box (3 stories) + 10 metric boxes (each: metric name and value at 28-30px, 3 beginner bullets with coral circles, chart panel with 1Y/2Y/5Y/All toggles, glossary block); FX metric box now also surfaces country.fxRegime.beginner as an orange-tinted callout (field already in data.json for all 12 countries; no migration needed). Killed in v2: mod/expert tiers from UI (backend unchanged), all weather icons, third-click dashboard layer. Stubs: favicon/OG/analytics, mod/expert backend generation, daily ritual rebuild of v2 (manual for now). MACRO-MAY/build.py shipped with --target flag (local writes to MACRO-MAY/dist/, production writes to ../v2/ for parallel deploy). Production cutover plan: live at /v2/ for several weeks of tightening, then v1 retires; pipeline scripts unchanged throughout. Service worker discovery: fundaxhosa SW on localhost scope was hijacking dev server; workaround is 127.0.0.1 (different origin), permanent fix is DevTools to Application to Service Workers to Unregister. Repo cleanup deferred: 28 stale HEADLINES_*/METRICS_*/harvest_* JSONs (2026-04-27 to 2026-05-07) show as deleted in git status, to be added to .gitignore + git rm --cached in a separate session. Local server: bash serve.sh into [http://127.0.0.1:8765/](http://127.0.0.1:8765/). Production deploy: python3 MACRO-MAY/build.py --target=production && git add v2/ && git commit && git push.
Session 80: Daily ritual completed 2026-05-07. morning_bash.sh ran in 4m 36s. 3 commodity rewrites (WTI -5.7% to 92.93, Brent -11.1% to 99.11, Silver +8.5% to 80.08). Headlines 13/13 in 187s. Metric stories: 21 regenerated, 111 carried forward (84% saved), 12/12 in 15s. Headline review gate passed. Build successful. Audit clean. Procedural change: Social Media Bash retired permanently from Daily Bash Ritual; section removed from brief; digest_server.py and digest_ui.html remain in the repo as inert artefacts. France Stock Market YTD homepage null flagged at session start; today's fetch_market_data.py wrote FRA +1.3% local / +1.47 USD cleanly and the build pushed the fix to the homepage; root cause parked. Several large metric-story trigger moves observed (FRA 130%/147%, ZAF 139%/180%, DEU 50%/62%, JPN 41%/47%, RUS 80%/9.8%) consistent with stale/null snapshots, supporting the FRA observation. 7 FRED bond/yield endpoints returned HTTP 500 (USA DGS10, USA TB3MS, JPN x2, FRA ECBDFR, ZAF x2); affected fields retain prior day's values; transient. Cosmetic: fetch_market_data.py summary counter undercounts failures (7 actual, says 1).
Session 79: Daily ritual completed 2026-04-29. New tooling: morning_bash.sh shipped, consolidates pre-review steps 1-6 of the Daily Bash Ritual into a single script with set -e (halts on first error), step banners, tee'd log to logs/morning_bash_YYYY-MM-DD.log, Friday forecast_server.py confirmation prompt with hard exit on "n", and runtime stamp. First production run completed in 5m 15s, zero errors, 12/12 MARKET-STATS countries appended cleanly. 4 commodity rewrites (WTI +11.4%, Brent +6.0%, Gold +5.9%, Wheat +8.1%). Headlines 13/13 in 205s. Metric stories: 20 regenerated, 112 carried forward (84% saved), 24s. Headline review gate passed. Build successful, auto-committed and pushed to master. Audit clean. Cosmetic noted for future tidy: update_metric_stories.py end-of-run "Next steps" output still references the retired METRICS_approved convention; should be tidied to reference draft file directly per Session 78. Daily Bash Ritual section in brief updated to show new bash morning_bash.sh entry point.
Session 78: Daily ritual completed 2026-04-28. First production run of Session 76 trigger-based metric story system - 17 regenerated, 115 carried forward (87% saved), 12/12 countries in 13s. 1 commodity story rewrite (Silver, moved 6.4%, was 78.86 now 73.83). Headlines 13/13 in 200s. Both review gates passed. Build successful, pushed to master. Audit clean. DEU MARKET-STATS sheet append failed in step 1 with Google 502; retried via fetch_market_data.py rerun and DEU + CHN both appended (idempotent skip caught a quietly-missing CHN row from an earlier session; net 2 appended, 10 skipped, 0 failed). Intraday data.json delta after retry left as-is per Ralph's call. Procedural change: Manual Gate 2 (metric stories review via metric_story_review.html) RETIRED. New sequence: update_metric_stories.py -> update_metric_stories.py --apply METRICS_draft_YYYY-MM-DD.json. No review UI, no copy/rename to "approved". Manual Gate 1 (headlines) remains in place. Daily Bash Ritual section in brief updated.
Session 77: Bug fix - USA Stock Market YTD homepage glitch (showed +4.20% local / -3.12% USD, an embarrassing mismatch since USD return must equal local for USA by definition). Root cause: metrics.market['Stock Market YTD (USD)'] was only written by sync_sheet.py --market (not in any scheduled ritual); field had been stale since 2026-03-22. Fix: USD-YTD computation moved into fetch_market_data.py as a derived metric. USA hard-coded to mirror local YTD string verbatim; other countries computed via FX YTD compound (1 + local%) * (1 + USD-per-local%) - 1 using yf_price_and_ytd() and Yahoo ticker direction (LOCALUSD=X gives USD-per-local, USDLOCAL=X gives local-per-USD requiring inversion). RULE 15 added (USA col2 = col1, no separate calculation). GitHub HTTPS auth refreshed via gh auth login after PAT expiry in macOS Keychain (gh 2.91.0 installed via Homebrew). Pushed to origin master.
Session 76: Daily cost review and trigger-based metric story regeneration shipped. Replaces "regenerate all 132 metrics daily" with per-metric triggers (new monthly print, daily-tier move 5%+ relative to snapshot, or staleness past tier ceiling 7/14/30d); anything else carried forward at zero API cost. New fields story_last_updated, story_value_snapshot, story_last_print_month added to data.json. backfill_story_freshness.py ran (132 stamped, 35 with print month). update_metric_stories.py rewritten (per-country prompt requesting only to-regen metrics, --force-all escape hatch, modified apply step that preserves carried-forward stamps). metric_story_review.html rewritten with carry/regen badges, status filter, header counter. Daily Bash Ritual unchanged. Expected 75-85% Haiku call reduction steady state. Headlines and IMF forecast swap parked for separate sessions.
Session 75: Cards system Phase 1 built end-to-end (static cards working for all 13 PNGs from live data.json) then entire face / cards / animation concept aborted by Ralph. Animation synthesis (ping-pong sequencing + per-frame jitter on sparse 3-frame library) couldn't substitute for hand-drawn variation. All Session 75 scripts (render_cards.py, card_config.py, card_text_draft.json, prep_faces.py, make_animated_previews.py, make_animated_cards.py, sync_face_library.py), faces/ directory (72 PNGs), and fonts/ directory removed from repo. Existing dashboard architecture preserved unchanged. Cards system design spec from Session 74 deleted from active brief sections.
Session 74: Strategy / design session. No code, no ritual, no commits. Decision taken to redesign the site around hand-drawn faces (24-face library, 3 boil variants each, drawn by Ralph). Country card and homepage mocks built and approved. Three-layer site architecture locked: homepage 12-face grid → country card → existing dashboard. Per-country-per-day permalinks. Metric rotation rule defined. 14 absolute operating rules formalised. "Lisa" clarified as computer name only; Ralph is the sole operator.
Session 73: Daily ritual completed 2026-04-24; 0 commodity stories rewritten (all within threshold); headlines 13/13 in 177s; metric stories 12/12 in 78s, no retries; all checks passed; Social Media Bash skipped per Ralph's instruction. No procedural changes - URL clickable-link rule reconfirmed and added to Claude memory.
Session 72: Daily ritual completed 2026-04-16; 4 commodity stories rewritten (WTI -7.2%, Silver +7.4%, Copper +5.0%, Wheat +6.9%); headlines 13/13 in 188s; metric stories 12/12 in 80s, no retries; all checks passed. No procedural changes.
Session 71: Daily ritual completed 2026-04-12; both review UIs (headline_review.html, metric_story_review.html) patched to save approved JSON directly to repo via showSaveFilePicker (File System Access API) with ~/Downloads/ fallback (patch_review_export.py).
Session 70: Daily ritual completed 2026-04-10; Copper and Wheat commodity stories rewritten; Google Sheets 429 quota hit on first sync_market_historical.py run - resolved by waiting 60s and rerunning.
Session 69: Daily ritual completed 2026-04-09; sync_edu.py and macedu-v2 push removed from Daily Bash Ritual permanently.
Session 68: Daily ritual completed 2026-04-08; header date fix (macrosnaps-shell.html top-left "Updated" now uses browser clock UTC, not stale data._meta.generated); sync_edu.py now writes to BRAINsmoothie as well as macedu-v2; Daily Bash Ritual updated to push BRAINsmoothie.
Session 67: Daily ritual completed 2026-04-07; macedu-v2 confirmed as live edu repo (sync_edu.py writes to macedu-v2/app/data/metrics.js); Social Media Bash ritual instructions updated (stop http.server first; open digest_ui.html directly).
Session 66: _frozen_historical date alignment architecture fixed (startDate added to all 86 monthly series; JS updated to left-align from startDate); Inflation (CPI) historical rebuilt from MACRO-MONTHLY sheet for all 12 countries (rebuild_cpi_historical.py); sync_monthly_actuals.py date format bug fixed (DD/MM/YYYY[:7] → strptime YYYY-MM); IND 10Y Bond Yield and Yield Curve flagged for FRED backfill.
Session 65: Daily Bash Ritual updated - mv commands added after each review gate for HEADLINES_approved and METRICS_approved files.
Session 64: Intraday Bash Ritual added (update_global_stories.py built; ad hoc mid-day refresh procedure documented).
Session 63: Daily ritual completed 2026-04-02; build.py timezone fix (UTC → Europe/London for date stamp); http.server must run from macrosnaps directory for review UIs.
Session 62: Daily ritual completed 2026-03-31; sync_edu.py path fix (runs from macrosnaps repo, not macedu); Daily Bash Ritual updated.
Session 61: Daily ritual completed 2026-03-29; commodity stories migrated to bullet arrays; sync_market_historical.py/audit_ritual.py/build.py bug fixes; Brent Crude backfill complete; macedu deployed to Cloudflare Pages; commodity story prompt overhauled.
Session 60: MARKET-STATS daily append fixed (fetch_market_data.py patched; yf_ytd_and_level() added); Mar 16-27 backfill run across 12 country tabs (backfill_market_stats.py); bond yield patch script built (patch_bond_yields.py); MACRO-MONTHLY unemployment backfilled for IND/ZAF (backfill_unemployment.py) and BRA (update_bra_unemployment.py); BRA IBGE SIDRA wired into update_monthly_actuals.py; historical unemployment and policy rate gaps filled via backfill_historical_gaps.py; backfill --apply run (13 cells); MACRO_MONTHLY_SHEET_ID added to .env.
Session 59: Metric story pipeline fixed (Haiku truncation → 1 country per call → parallelised to 12 concurrent calls, 990s → 74s); JSON file renames (HEADLINES/METRICS); headline_review.html patched for bullets schema; build.py validator updated; Daily Bash Ritual updated.
Session 58: Forecast CMS added (forecast_server.py + forecast_cms.html); Friday pre-ritual rule added to Daily Bash Ritual.
Session 57: Favicon added (favicon.ico + favicon-192.png); favicon link tags added to macrosnaps-shell.html; X Card Validator confirmed icon picked up.
Session 56: Repo moved to /Users/lisaswerling/RALPH/AI/macrosnaps; MARKET_STATS_KEY_FILE env var added; node_modules scrubbed from macrosnaps and macedu repos; Daily Bash Ritual fixed (sync_edu.py before audit_ritual.py; macedu push added).
Session 55: macroeconomics.education data layer complete. sync_edu.py added to Daily Bash Ritual. macedu pushed to GitHub.
Session 54: US and UK educator outreach campaigns complete/paused per Ralph's instruction.
Session 53: US educator outreach ranks 55-51 complete; 10 contacts, 5 universities (Georgia Tech skip confirmed); LIVING_BRIEF updated; session prompt for ranks 50-1 created.
Session 52: Teaching-staff-first rule formalised; session prompt for ranks 55-1 revised.
Session 51: US educator outreach ranks 70-56 complete; 59 contacts, 15 universities; 3 CSV files produced.
Session 50: Social Media Bash documented; local preview rule added to standing rules.
Session 49: US educator outreach ranks 100-71 complete; 76 contacts, 30 universities; 5 CSV files produced.
Session 48: UK top 20 educator outreach complete (178 contacts); US top 100 outreach plan built.
Session 47: Educator cold email outreach campaign built; UK university macro contacts database started ranks 20-13.
Session 46: educators.html created; Twitter/OG meta tags added; Subscribe + Educators buttons moved to footer; #ping hash handler added; educator outreach strategy added to brief.
Session 45: build.py structural date fix; US to Commodities nav flash fixed.
[Earlier sessions: see prior LIVING_BRIEF history for complete record]
